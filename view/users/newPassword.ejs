<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Password Reset</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      background-color: #f2f2f2; /* Background color */
    }

    div {
      text-align: center;
      max-width: 400px;
      margin: auto;
      background-color: #fff; /* Form background color */
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1); /* Box shadow for a subtle lift */
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    form {
      margin-top: 20px;
    }

    label {
      display: block;
      margin-bottom: 10px;
      color: #555;
      text-align: left;
      font-size: 14px;
    }

    input {
      width: 100%;
      padding: 12px;
      margin-bottom: 20px;
      box-sizing: border-box;
      border: 1px solid #ccc; /* Input border color */
      border-radius: 5px;
      font-size: 16px;
    }

    button {
      background-color: #530404; /* Button background color */
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }

    button:disabled {
      background-color: #b8b8b8;
      cursor: not-allowed;
    }

    button:hover {
      background-color: #088178; /* Darker shade on hover */
    }
    .text-danger{
      color: red;

    }
  </style>
</head>
<body>

<div>
  <h1>Reset Password</h1>
  <form action="/newPassword" method="post" >
    <%if(locals.message){%>
      <span class="text-danger"><%=message%></span>

    <%}%>
    <label for="newPassword">New Password:</label>
    <input type="password" id="newPassword" name="newPassword" required>
    
    <label for="confirmPassword">Confirm Password:</label>
    <input type="password" id="confirmPassword" name="confirmPassword" required>
    
    <button type="submit" id="resetButton" >Reset Password</button>
  </form>
</div>
     
</body>
</html>
